# Run ibdEstM in parallel

Run ibdEstM in parallel

## Usage

``` r
run_ibdestm(
  dsmp,
  coi,
  afreq,
  sample_pairs = NULL,
  pval = TRUE,
  confint = FALSE,
  rnull = 0,
  alpha = 0.05,
  total_cores = NULL,
  verbose = FALSE
)
```
