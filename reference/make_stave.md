# Group variantstring-style mutations into stave format

Group variantstring-style mutations into stave format

## Usage

``` r
make_stave(variant)
```

## Arguments

- variant:

  Character string of `;`-separated `gene:position:mutation`.

## Value

A stave-formatted string.
