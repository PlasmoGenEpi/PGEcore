# Run the vendored Multi-Loci Biallelic Model MLE

Run the vendored Multi-Loci Biallelic Model MLE

## Usage

``` r
run_MultiLociBiallelicModel(inputToMLE)
```

## Arguments

- inputToMLE:

  Matrix/data prepared for the vendored `mle()` function.

## Value

List with `lambda`, `plsf_table`, and `runtime`.
