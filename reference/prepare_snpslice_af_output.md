# Format SNP-Slice allele frequencies with variantstring names

Format SNP-Slice allele frequencies with variantstring names

## Usage

``` r
prepare_snpslice_af_output(snpslice_res, loci_groups, estimator)
```
