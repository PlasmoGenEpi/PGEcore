# Prevalence and frequency of target variants vs a dataset

Prevalence and frequency of target variants vs a dataset

## Usage

``` r
calculate_variant_prevalence(target_variants, comparison_variants)
```

## Arguments

- target_variants:

  Target variant strings.

- comparison_variants:

  Dataset variant strings.

## Value

A data frame with `variant`, `prev`, `freq`, `sample_total`.
