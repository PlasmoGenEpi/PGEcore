# Does the installed FreqEstimationModel accept a seed?

Checks `mcmc_sampling_parallel()`'s formals for a `seed` argument.

## Usage

``` r
fem_supports_seed()
```

## Value

`TRUE` when `mcmc_sampling_parallel()` has a `seed` argument.
