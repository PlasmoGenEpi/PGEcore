# Calculate multilocus prev/freq weighted by WSAF

Calculate multilocus prev/freq weighted by WSAF

## Usage

``` r
calculate_multilocus_af_prev_wsaf_prop(multilocus_calls)
```

## Arguments

- multilocus_calls:

  Table with `specimen_name`, `variant`, `wsaf`.

## Value

A tibble with `prev` and `freq`.
