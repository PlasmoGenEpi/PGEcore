# Read a COI table into a named vector aligned with a Dcifer allele list

Read a COI table into a named vector aligned with a Dcifer allele list

## Usage

``` r
create_dcifer_coi_input(
  coi_path,
  allele_list,
  specimen_name_col = "specimen_name"
)
```
