# Core greedy filter on expected heterozygosity and distance

Core greedy filter on expected heterozygosity and distance

## Usage

``` r
filter_highest_diversity_snps_core(
  snp_table_in,
  mindist_between_snps = 10000,
  only_biallelic = FALSE,
  only_informative = FALSE
)
```
