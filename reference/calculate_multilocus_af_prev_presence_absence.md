# Calculate multilocus prev/freq by presence/absence

Calculate multilocus prev/freq by presence/absence

## Usage

``` r
calculate_multilocus_af_prev_presence_absence(multilocus_calls)
```

## Arguments

- multilocus_calls:

  Table with `specimen_name`, `variant`.

## Value

A tibble with `prev` and `freq`.
