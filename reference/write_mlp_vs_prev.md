# Write a variantstring prev/freq table

Write a variantstring prev/freq table

## Usage

``` r
write_mlp_vs_prev(df_prev, output)
```

## Arguments

- df_prev:

  Data frame with `group_id`, `variant`, `prev`, `freq`, `sample_total`.

- output:

  Output TSV path.
