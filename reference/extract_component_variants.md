# Extract unambiguous component genotypes from variant strings

Extract unambiguous component genotypes from variant strings

## Usage

``` r
extract_component_variants(variant_strings)
```

## Arguments

- variant_strings:

  Character vector of variant strings.

## Value

Unique component variant strings with no heterozygous loci.
