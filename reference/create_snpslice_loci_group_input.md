# Read loci groups for SNP-Slice multilocus frequencies

Read loci groups for SNP-Slice multilocus frequencies

## Usage

``` r
create_snpslice_loci_group_input(
  loci_groups_path,
  allele_table,
  target_name_col = "target_name"
)
```
