# TRUE if `x` is a single canonical DNA base (a simple-SNP allele)

TRUE if `x` is a single canonical DNA base (a simple-SNP allele)

## Usage

``` r
is_simple_base(x)
```

## Arguments

- x:

  Character string.

## Value

Logical.
