# Extract the TARGET= value from a VCF INFO string, or NA if absent

Extract the TARGET= value from a VCF INFO string, or NA if absent

## Usage

``` r
info_target(info)
```

## Arguments

- info:

  VCF INFO field.

## Value

Character scalar or `NA_character_`.
