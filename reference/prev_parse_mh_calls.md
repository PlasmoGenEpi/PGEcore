# Load microhaplotype calls for naive allele prevalence estimation

Load microhaplotype calls for naive allele prevalence estimation

## Usage

``` r
prev_parse_mh_calls(path)
```

## Arguments

- path:

  Path to a TSV of microhaplotype genotypes.

## Value

A tibble with columns including `specimen_name`, `target_name`, and
`variant`.
