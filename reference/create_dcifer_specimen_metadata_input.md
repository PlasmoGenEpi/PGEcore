# Read specimen metadata for population-specific Dcifer runs

Read specimen metadata for population-specific Dcifer runs

## Usage

``` r
create_dcifer_specimen_metadata_input(
  specimen_metadata_path,
  coi,
  specimen_name_col = "specimen_name",
  pop_name_col = "population"
)
```
