# Run ibdPair in parallel

Run ibdPair in parallel

## Usage

``` r
run_ibdpair(
  dsmp,
  coi,
  afreq,
  sample_pairs = NULL,
  pval = TRUE,
  confint = FALSE,
  rnull = 0,
  alpha = 0.05,
  nr = 1000,
  reval = NULL,
  total_cores = NULL,
  verbose = FALSE
)
```
