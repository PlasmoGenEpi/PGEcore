# Orient an allele sequence to the plus strand of its target

Orient an allele sequence to the plus strand of its target

## Usage

``` r
orient_allele_seq_to_plus_strand(seq, strand)
```

## Arguments

- seq:

  Character allele sequence.

- strand:

  Target strand (`"+"` or `"-"`).

## Value

A `DNAString`.
