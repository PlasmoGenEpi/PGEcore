# Check that an object matches the expected COI estimate format

Check that an object matches the expected COI estimate format

## Usage

``` r
check_coi_format(df_coi)
```

## Arguments

- df_coi:

  A data frame of COI estimates.

## Value

Invisibly returns `TRUE` if valid.
