# Read amino acid calls for naive multilocus prev/freq

Read amino acid calls for naive multilocus prev/freq

## Usage

``` r
read_mlp_naive_aa_table(aa_calls)
```

## Arguments

- aa_calls:

  Path to a TSV of amino acid calls.

## Value

A tibble including `n_aa` (allele count per specimen/locus).
