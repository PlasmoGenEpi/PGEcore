# Validate panel reference BED columns and types

Validate panel reference BED columns and types

## Usage

``` r
validate_ref_bed_table(ref_bed, data_name = "ref_bed")
```

## Arguments

- ref_bed:

  Data frame of panel locations.

- data_name:

  Label used in error messages.

## Value

Invisibly returns `TRUE` if valid.
