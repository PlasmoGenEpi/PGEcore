# Load amino acid calls for naive allele frequency estimation

Load amino acid calls for naive allele frequency estimation

## Usage

``` r
af_parse_aa_calls(path)
```

## Arguments

- path:

  Path to a TSV of amino acid calls.

## Value

A tibble with columns `specimen_name`, `target_name`, `reads`, and
`variant`.
