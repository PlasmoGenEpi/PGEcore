# Parse a comma-separated name list or a one-column TSV of names

Parse a comma-separated name list or a one-column TSV of names

## Usage

``` r
parse_name_list_arg(value)
```

## Arguments

- value:

  Character scalar, or `NULL`.

## Value

Character vector of names (possibly empty).
