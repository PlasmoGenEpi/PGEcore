# Load amino acid calls for naive allele prevalence estimation

Load amino acid calls for naive allele prevalence estimation

## Usage

``` r
prev_parse_aa_calls(path)
```

## Arguments

- path:

  Path to a TSV of amino acid calls.

## Value

A tibble with columns `specimen_name`, `target_name`, and `variant`.
