# Check several suggested packages

Check several suggested packages

## Usage

``` r
check_suggested_pkgs(pkgs, reason = NULL)
```

## Arguments

- pkgs:

  Character vector of package names.

- reason:

  Optional short description of why they are needed.

## Value

Invisibly returns `TRUE` if all packages are available.
