# Load multi-locus allele frequency (MLAF) data

Load multi-locus allele frequency (MLAF) data

## Usage

``` r
load_mlaf(path)
```

## Arguments

- path:

  Path to a TSV with columns `group_id`, `variant`, and `freq`.

## Value

A tibble with those three columns.
