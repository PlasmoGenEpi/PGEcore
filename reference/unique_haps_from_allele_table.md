# Unique haplotypes by target_name and seq

Unique haplotypes by target_name and seq

## Usage

``` r
unique_haps_from_allele_table(allele_table)
```

## Arguments

- allele_table:

  Allele table with `target_name` and `seq`.

## Value

Distinct rows sorted by `target_name`.
