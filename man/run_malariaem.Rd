% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/malariaem_wrapper.R
\name{run_malariaem}
\alias{run_malariaem}
\title{Run malaria.em and write frequency and phase summaries}
\usage{
run_malariaem(
  matrix,
  test_size = "min",
  max_size = "8",
  subset_targets = FALSE,
  target_groups = NULL,
  freq_output = NULL,
  phase_output = NULL
)
}
\arguments{
\item{matrix}{Allele matrix (specimens as rows, loci as columns). See
\emph{Inputs}.}

\item{test_size}{Maximum COI to test, or \code{"min"} for the minimum allowed.}

\item{max_size}{Error if inferred COI range exceeds this cutoff.}

\item{subset_targets}{If \code{TRUE}, run separately for each \code{group_id}.}

\item{target_groups}{Data frame with \code{group_id} and \code{target_name}. See
\emph{Inputs}.}

\item{freq_output}{Path for genotype-frequency TSV. See \emph{Outputs}.}

\item{phase_output}{Path for phasing TSV. See \emph{Outputs}.}
}
\value{
A list of malaria.em results (or a named list per group).
}
\description{
Runs malaria.em haplotype EM on an in-memory allele matrix and writes
genotype-frequency and phasing summaries. Requires \strong{malaria.em} (Suggests).
For reading allele tables from disk, use \code{\link[=malariaem_wrapper]{malariaem_wrapper()}}.
}
\details{
\subsection{Inputs}{
\itemize{
\item \strong{\code{matrix}}: Allele matrix (specimens as rows, loci as columns; alleles
space-separated within cells).
\item \strong{\code{target_groups}}: Optional data frame with \code{group_id} and \code{target_name}
when \code{subset_targets = TRUE}.
}
}

\subsection{Outputs}{
\itemize{
\item \strong{\code{freq_output}}: Genotype-frequency TSV (\code{gt_id}, \code{target_name}, \code{seq},
\code{freq}, \code{freq_se}; plus \code{group_id} when subsetting).
\item \strong{\code{phase_output}}: Phasing TSV (\code{specimen_name}, \code{target_name}, \code{seq},
\code{gt_id}, \code{posterior_est}, \code{phase_id}; plus \code{group_id} when subsetting).
}
}

\subsection{Running}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{run_malariaem(
  matrix = allele_matrix,
  freq_output = "gt_freq_summary_all.tsv",
  phase_output = "gt_phase_summary_all.tsv"
)
}\if{html}{\out{</div>}}

File and CLI users should call \code{\link[=malariaem_wrapper]{malariaem_wrapper()}} /
\verb{Rscript exec/malariaem_wrapper ...}.

Requires \strong{malaria.em} (Suggests).
}
}
\seealso{
\code{\link[=malariaem_wrapper]{malariaem_wrapper()}},
\code{vignette("input-formats", package = "PGEcore")}
}
