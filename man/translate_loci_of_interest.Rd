% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translate_loci_of_interest.R
\name{translate_loci_of_interest}
\alias{translate_loci_of_interest}
\title{Translate loci of interest from microhaplotype sequences}
\usage{
translate_loci_of_interest(
  allele_table,
  ref_bed,
  loci_of_interest,
  output_directory,
  select_target_names = NULL,
  select_specimen_names = NULL,
  overwrite_dir = FALSE,
  output_stop_codons = FALSE,
  collapse_calls_by_summing = FALSE
)
}
\arguments{
\item{allele_table}{Path or data frame with columns \code{specimen_name},
\code{target_name}, \code{reads}, and \code{seq}.}

\item{ref_bed}{Path or data frame with columns \verb{#chrom}, \code{start}, \code{end},
\code{target_name}, \code{length}, \code{strand}, and \code{ref_seq}.}

\item{loci_of_interest}{Path or data frame with columns \verb{#chrom}, \code{start},
\code{end}, \code{name}, \code{length}, \code{strand}, \code{gene}, \code{gene_id}, and \code{aa_position}.
Each locus must have \code{length == 3}.}

\item{output_directory}{Directory to write results. Created if missing.}

\item{select_target_names}{Optional comma-separated names, path to a
one-column TSV, or character vector of targets to keep.}

\item{select_specimen_names}{Optional comma-separated names, path to a
one-column TSV, or character vector of specimens to keep.}

\item{overwrite_dir}{If \code{FALSE} (default), refuse to replace an existing
\code{output_directory}.}

\item{output_stop_codons}{If \code{FALSE} (default), treat \code{*} as untranslatable
(along with \code{X}).}

\item{collapse_calls_by_summing}{If \code{TRUE}, sum reads across overlapping
targets; otherwise keep the target with the highest read count.}
}
\value{
A named list with \code{loci_of_interest_for_target_for_microhap},
\code{amino_acid_calls}, \code{collapsed_amino_acid_calls},
\code{loci_covered_by_target_samples_info}, and \code{untranslatable}.
}
\description{
Aligns each unique haplotype to its panel reference with an overlap
pairwise alignment, extracts the codon at each locus of interest, and
translates it. Requires \strong{Biostrings} and \strong{pwalign} (Suggests).
}
\details{
Writes \code{loci_of_interest_for_target_for_microhap.tsv.gz},
\code{amino_acid_calls.tsv.gz}, \code{collapsed_amino_acid_calls.tsv.gz},
\code{loci_covered_by_target_samples_info.tsv}, and optionally
\code{allele_table_out_untranslatable.tsv} under \code{output_directory}.
}
