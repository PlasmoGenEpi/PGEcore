% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/THEREALMcCOIL_wrapper.R
\name{run_mccoil_chain}
\alias{run_mccoil_chain}
\title{Run a single McCOIL chain and return its per-iteration trace}
\usage{
run_mccoil_chain(
  mccoil_input,
  model,
  maxCOI,
  threshold_ind,
  threshold_site,
  totalrun,
  burnin,
  M0,
  e1,
  e2,
  epsilon,
  err_method,
  seed,
  work_dir,
  output
)
}
\arguments{
\item{mccoil_input}{Prepared model input: the genotype matrix for the
categorical model, or a list with \code{a1}/\code{a2} matrices for the proportional
model.}

\item{model}{\code{"categorical"} or \code{"proportional"}.}

\item{maxCOI}{Upper bound for COI.}

\item{threshold_ind}{Minimum sites per sample (categorical model).}

\item{threshold_site}{Minimum samples per locus (categorical model).}

\item{totalrun}{Total MCMC iterations.}

\item{burnin}{Burn-in iterations.}

\item{M0}{Initial COI.}

\item{e1}{Probability of calling homozygous loci heterozygous (categorical).}

\item{e2}{Probability of calling heterozygous loci homozygous (categorical).}

\item{epsilon}{Error parameter for the proportional model.}

\item{err_method}{\code{1}: treat error rates as constants; \code{3}: estimate them
with COI and allele frequencies.}

\item{seed}{Random seed for this chain.}

\item{work_dir}{Directory for McCOIL temp traces.}

\item{output}{Trace filename (under \code{work_dir}) for this chain.}
}
\value{
A data frame of the raw per-iteration trace, one row per iteration
plus the trailing acceptance-count row.
}
\description{
Runs one MCMC chain of the requested model with a given seed, writing its
trace and summary to \code{output} under \code{work_dir}, and reads the trace back.
}
\keyword{internal}
