% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_ref_seqs_with_full_genome_ref_fasta.R
\name{add_ref_seqs_with_full_genome_ref_fasta}
\alias{add_ref_seqs_with_full_genome_ref_fasta}
\title{Add reference sequences extracted from a genome FASTA onto a panel BED table}
\usage{
add_ref_seqs_with_full_genome_ref_fasta(
  ref_bed,
  genome_fasta,
  out = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{ref_bed}{Path to a TSV BED table with a header, or a data frame, with
columns \verb{#chrom}, \code{start}, \code{end}, \code{target_name}, \code{length}, and \code{strand}.}

\item{genome_fasta}{Path to a genome FASTA.}

\item{out}{Optional output TSV path. If \code{NULL}, results are returned without
writing.}

\item{overwrite}{If \code{FALSE} (default), refuse to overwrite \code{out}.}
}
\value{
A tibble of \code{ref_bed} with a \code{ref_seq} column.
}
\description{
For each BED interval, extracts \code{length} bases starting at 0-based \code{start}
(Biostrings 1-based \code{start + 1}) from the contig named in \verb{#chrom}. Minus-
strand intervals are reverse-complemented. Requires \strong{Biostrings} (Suggests).
Genome FASTA names are truncated at the first whitespace.
}
