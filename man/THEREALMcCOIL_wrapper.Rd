% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/THEREALMcCOIL_wrapper.R
\name{THEREALMcCOIL_wrapper}
\alias{THEREALMcCOIL_wrapper}
\title{Estimate COI and allele frequencies with THEREALMcCOIL}
\usage{
THEREALMcCOIL_wrapper(
  snp_calls_input,
  slaf_output,
  coi_output,
  model = "categorical",
  maxCOI = 25L,
  threshold_ind = 20L,
  threshold_site = 20L,
  totalrun = 10000L,
  burnin = 1000L,
  M0 = 15L,
  e1 = 0.05,
  e2 = 0.05,
  epsilon = 0.02,
  err_method = 1L,
  seed = 321L,
  n_chains = 3L,
  convergence_output = "convergence_diag.tsv"
)
}
\arguments{
\item{snp_calls_input}{TSV of SNP calls with at least \code{specimen_name},
\code{snp_name}, \code{pos}, \code{seq_base}, \code{reads} (and typically \code{target_name}, \code{he}).}

\item{slaf_output}{Output TSV of allele frequencies (\code{variant}, \code{freq}).}

\item{coi_output}{Output TSV of COI estimates (\code{specimen_name}, \code{coi}).}

\item{model}{\code{"categorical"} (heterozygous/homozygous calls) or
\code{"proportional"} (allele frequency / read-count data).}

\item{maxCOI}{Upper bound for COI.}

\item{threshold_ind}{Minimum sites per sample (categorical model).}

\item{threshold_site}{Minimum samples per locus (categorical model).}

\item{totalrun}{Total MCMC iterations.}

\item{burnin}{Burn-in iterations.}

\item{M0}{Initial COI.}

\item{e1}{Probability of calling homozygous loci heterozygous (categorical).}

\item{e2}{Probability of calling heterozygous loci homozygous (categorical).}

\item{epsilon}{Error parameter for the proportional model.}

\item{err_method}{\code{1}: treat error rates as constants; \code{3}: estimate them
with COI and allele frequencies.}

\item{seed}{Random seed for the first chain; chain \code{i} uses \code{seed + i - 1}.}

\item{n_chains}{Number of independent MCMC chains. More than one chain is
required for the Gelman-Rubin R-hat diagnostic.}

\item{convergence_output}{Output TSV of MCMC convergence diagnostics, one
row per parameter.}
}
\value{
A list with \code{slaf}, \code{coi} and \code{convergence} (invisibly after writing
outputs).
}
\description{
File-oriented entry point used by the \code{THEREALMcCOIL_wrapper} CLI. Compiled
C MCMC routines (\code{McCOIL_categorical}, \code{McCOIL_prop}) are linked at package
install time.
}
