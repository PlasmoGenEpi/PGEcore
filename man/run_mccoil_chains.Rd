% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/THEREALMcCOIL_wrapper.R
\name{run_mccoil_chains}
\alias{run_mccoil_chains}
\title{Run several independent McCOIL chains into \code{work_dir}}
\usage{
run_mccoil_chains(
  df,
  model = "categorical",
  maxCOI = 25,
  threshold_ind = 20,
  threshold_site = 20,
  totalrun = 10000,
  burnin = 1000,
  M0 = 15,
  e1 = 0.05,
  e2 = 0.05,
  epsilon = 0.02,
  err_method = 1,
  seed = 321,
  n_chains = 3,
  work_dir,
  output = "McCOIL_out.txt"
)
}
\arguments{
\item{df}{Preprocessed SNP calls.}

\item{model}{\code{"categorical"} or \code{"proportional"}.}

\item{maxCOI}{Upper bound for COI.}

\item{threshold_ind}{Minimum sites per sample (categorical model).}

\item{threshold_site}{Minimum samples per locus (categorical model).}

\item{totalrun}{Total MCMC iterations.}

\item{burnin}{Burn-in iterations.}

\item{M0}{Initial COI.}

\item{e1}{Probability of calling homozygous loci heterozygous (categorical).}

\item{e2}{Probability of calling heterozygous loci homozygous (categorical).}

\item{epsilon}{Error parameter for the proportional model.}

\item{err_method}{\code{1}: treat error rates as constants; \code{3}: estimate them
with COI and allele frequencies.}

\item{seed}{Random seed for the first chain; chain \code{i} uses \code{seed + i - 1}.}

\item{n_chains}{Number of independent MCMC chains. More than one chain is
required for the Gelman-Rubin R-hat diagnostic.}

\item{work_dir}{Directory for McCOIL temp traces (typically under
\code{tempdir()}).}

\item{output}{Base filename for chain 1, written under \code{work_dir}; later
chains append a \verb{_chain<i>} suffix.}
}
\value{
A list with \code{traces} (per-chain trace data frames, chain 1 first)
and \code{summary_path}, the summary file written by chain 1.
}
\description{
Prepares the model input once, then runs \code{n_chains} chains with seeds
\verb{seed, seed + 1, ...}. Chains are independent and write to distinct trace
files in \code{work_dir}, so they can run in parallel forks; on Windows, where
forking is unavailable, they run sequentially.
}
\keyword{internal}
