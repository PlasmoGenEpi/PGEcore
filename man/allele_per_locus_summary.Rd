% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allele_per_locus_summary.R
\name{allele_per_locus_summary}
\alias{allele_per_locus_summary}
\title{Summarize alleles per locus from an allele table}
\usage{
allele_per_locus_summary(allele_table, output = "allele_summary_by_target.tsv")
}
\arguments{
\item{allele_table}{Path to an allele table TSV. See \emph{Inputs}.}

\item{output}{Optional output TSV path. Defaults to
\code{"allele_summary_by_target.tsv"}.}
}
\value{
A tibble with columns \code{target_name}, \code{total_allele_count},
\code{unique_allele_count}, and \code{allele_singlets}.
}
\description{
For each \code{target_name}, computes total allele count, unique allele count,
and the number of alleles that appear only once (singlets).
}
\details{
\subsection{Inputs}{
\itemize{
\item \strong{\code{allele_table}}: Allele table (\code{specimen_name}, \code{target_name}, \code{seq}).
See \code{vignette("input-formats", package = "PGEcore")}.
}
}

\subsection{Outputs}{
\itemize{
\item \strong{\code{output}} (optional): TSV with columns \code{target_name},
\code{total_allele_count}, \code{unique_allele_count}, and \code{allele_singlets}.
Defaults to \code{"allele_summary_by_target.tsv"}. If \code{NULL}, results are
returned without writing a file.
}
}

\subsection{Running}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{allele_per_locus_summary(
  allele_table = "allele_table.tsv",
  output = "allele_summary_by_target.tsv"
)
}\if{html}{\out{</div>}}

\if{html}{\out{<div class="sourceCode bash">}}\preformatted{Rscript exec/allele_per_locus_summary \\
  --allele_table allele_table.tsv \\
  --output allele_summary_by_target.tsv
}\if{html}{\out{</div>}}
}
}
\examples{
allele_path <- system.file(
  "extdata", "example_allele_table.tsv",
  package = "PGEcore"
)
allele_per_locus_summary(allele_path, output = NULL)

}
\seealso{
\code{vignette("input-formats", package = "PGEcore")}
}
