% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_coi_naive.R
\name{estimate_coi_naive}
\alias{estimate_coi_naive}
\title{Estimate COI using naive allele-count methods}
\usage{
estimate_coi_naive(
  allele_table,
  output = NULL,
  method = "integer_method",
  integer_threshold = 1,
  quantile_threshold = 0.05
)
}
\arguments{
\item{allele_table}{Path to a TSV of allele calls with columns
\code{specimen_name}, \code{target_name}, \code{reads}, and \code{seq}.}

\item{output}{Optional path to write a TSV with columns
\code{specimen_name} and \code{coi}. If \code{NULL}, results are returned without writing.}

\item{method}{One of \code{"integer_method"} or \code{"quantile_method"}.
Default: \code{"integer_method"}.}

\item{integer_threshold}{Positive integer index into the ordered allele
counts (integer method only). Default: \code{1}.}

\item{quantile_threshold}{Quantile in \verb{[0, 1]} (quantile method only).
Values near zero yield higher COI estimates. Default: \code{0.05}.}
}
\value{
A tibble with columns \code{specimen_name} and \code{coi}.
}
\description{
For every specimen, counts distinct alleles at each locus and sorts those
counts in decreasing order. With \code{method = "integer_method"}, the
\code{integer_threshold}-th value is the COI estimate. With
\code{method = "quantile_method"}, the value at \code{quantile_threshold} is used
instead (scaling naturally with the number of observed allele rows).
}
\examples{
allele_path <- system.file(
  "extdata", "example_allele_table.tsv",
  package = "PGEcore"
)
estimate_coi_naive(allele_path, method = "integer_method")

}
