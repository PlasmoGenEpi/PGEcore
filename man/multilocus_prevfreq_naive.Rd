% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multilocus_prevfreq_naive.R
\name{multilocus_prevfreq_naive}
\alias{multilocus_prevfreq_naive}
\title{Estimate multilocus prevalence and frequency with naive phasing}
\usage{
multilocus_prevfreq_naive(
  aa_calls,
  loci_groups,
  output = NULL,
  single_locus_output = NULL,
  method = "wsaf_prop",
  wsaf_cut_off = 0.7
)
}
\arguments{
\item{aa_calls}{Path to a TSV of amino acid calls with columns
\code{specimen_name}, \code{gene}, \code{gene_id}, \code{aa_position}, \code{reads}, and \code{aa}.}

\item{loci_groups}{Path to a TSV of loci groups with columns
\code{group_id}, \code{gene_id}, and \code{aa_position}.}

\item{output}{Optional path for the multilocus prev/freq TSV. If
\code{NULL}, results are returned without writing.}

\item{single_locus_output}{Optional path for single-locus
prev/freq recalculated from the inferred multilocus calls.}

\item{method}{\code{"wsaf_prop"} (default) or \code{"presence_absence"}.}

\item{wsaf_cut_off}{WSAF threshold used to infer a dominant haplotype when
more than one locus is heterozygous. Default: \code{0.70}.}
}
\value{
A tibble of multilocus prevalence and frequency estimates.
}
\description{
For each loci group, specimens that have calls at every locus are retained.
Haplotypes are inferred when exactly one locus is heterozygous, or when every
locus has a single allele above \code{wsaf_cut_off} (including monoclonal
samples). Prevalence and frequency are then estimated with \code{wsaf_prop} or
\code{presence_absence}.
}
\examples{
aa_path <- system.file(
  "extdata", "example2_aa_calls.tsv",
  package = "PGEcore"
)
groups_path <- system.file(
  "extdata", "example_loci_groups.tsv",
  package = "PGEcore"
)
multilocus_prevfreq_naive(aa_path, groups_path)

}
