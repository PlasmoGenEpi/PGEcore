# PGEcore 0.1.0

* Initial R package skeleton.
* Shared helpers for suggested packages and external executables.
* Migrated `count_samples_by_coi` (R API + `exec/` CLI).
* Migrated `coiaf_wrapper` / `run_coiaf` (optional Suggests dependency + `exec/` CLI).
